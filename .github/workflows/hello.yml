name: hello
on:
  push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: cachix/install-nix-action@v10
      with:
        nix_path: nixpkgs=channel:nixos-unstable
    - uses: actions/checkout@v2
      with:
        path: actions
    - uses: actions/checkout@v2
      with:
        repository: 'hypered/design-system'
        path: design-system
    - name: Run some inline shell script
      run: |
        echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
        ls "$GITHUB_WORKSPACE"
